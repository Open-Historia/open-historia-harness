{
  "name": "open-historia-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless test harness for Open Historia. Plays the game for real and reports state as data, with no UI and no screenshots.",
  "bin": {
    "oh-harness": "./cli.js"
  },
  "scripts": {
    "test": "node --test \"lib/**/*.test.js\"",
    "harness": "node cli.js",
    "self-test": "node cli.js --self-test"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
